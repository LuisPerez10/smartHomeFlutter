// Convertir array string a lista

// final regExp = new RegExp(r'(?:\[)?(\[[^\]]*?\](?:,?))(?:\])?');
// final input = '[[sometext],["122"],[411]]';
// final result = regExp.allMatches(input).map((m) => m.group(1))
//   .map((String? item) => item?.replaceAll(new RegExp(r'[\[\],]'), ''))
//   .map((m) => [m])
//   .toList();
// print(result);
